{
  "name": "commerce-app",
  "version": "1.0.0",
  "private": true,
  "description": "Production-ready e-commerce app with Express, MongoDB, JWT, Stripe, and Excel exports.",
  "scripts": {
    "install:all": "npm install --prefix server",
    "dev": "npm run dev --prefix server",
    "start": "npm start --prefix server"
  }
}
